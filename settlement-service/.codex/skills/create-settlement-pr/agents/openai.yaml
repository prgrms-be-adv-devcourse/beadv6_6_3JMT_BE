interface:
  display_name: "Create Settlement PR"
  short_description: "정산 요청을 범용 검증과 템플릿 기반 PR로 연결"
  default_prompt: "Use $create-settlement-pr to validate this branch and prepare a settlement pull request."
policy:
  allow_implicit_invocation: true
