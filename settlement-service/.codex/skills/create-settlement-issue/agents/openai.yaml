interface:
  display_name: "Create Settlement Issue"
  short_description: "정산 요청을 범용 템플릿 기반 이슈 생성으로 연결"
  default_prompt: "Use $create-settlement-issue to draft a settlement GitHub issue and create it after approval."
policy:
  allow_implicit_invocation: true
