interface:
  display_name: "Commit Settlement Changes"
  short_description: "정산 서비스 변경을 규칙에 맞춰 안전하게 커밋"
  default_prompt: "Use $commit-settlement-changes to inspect and commit the current settlement-service change."
policy:
  allow_implicit_invocation: true
