spring:
  application:
    name: settlement-service
  config:
    import: ${CONFIG_IMPORT:}
  profiles:
    default: local
  batch:
    job:
      enabled: false
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 1000
        max-interval: 2000
        multiplier: 1.1

api:
  init: /api/v1
