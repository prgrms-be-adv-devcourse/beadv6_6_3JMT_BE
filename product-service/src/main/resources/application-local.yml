server:
  port: 8082

spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:prompthub}?currentSchema=product_service
    username: ${DB_USERNAME:chan}
    password: ${DB_PASSWORD:123}
    driver-class-name: org.postgresql.Driver
  flyway:
    default-schema: product_service
    schemas: product_service
    create-schemas: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: product-service
      auto-offset-reset: earliest
      enable-auto-commit: false

grpc:
  server:
    port: ${GRPC_SERVER_PORT:9082}
  client:
    user-service:
      host: ${USER_SERVICE_GRPC_HOST:localhost}
      port: ${USER_SERVICE_GRPC_PORT:9081}

cloud:
  aws:
    region: ${AWS_REGION:ap-northeast-2}
    s3:
      bucket: ${AWS_S3_BUCKET:3jmt-prompthub-bucket}

eureka:
  client:
    enabled: false

logging:
  level:
    com.prompthub.product: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
