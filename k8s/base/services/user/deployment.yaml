apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: prompthub
    app.kubernetes.io/component: service
    app.kubernetes.io/managed-by: kustomize
spec:
  replicas: 1
  revisionHistoryLimit: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: user-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: user-service
        app.kubernetes.io/part-of: prompthub
        app.kubernetes.io/component: service
        app.kubernetes.io/managed-by: kustomize
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      terminationGracePeriodSeconds: 30
      nodeSelector:
        prompthub.io/node-pool: application
      imagePullSecrets:
        - name: ghcr-pull-secret
      initContainers:
        - name: wait-for-dependencies
          image: busybox:1.37.0
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -ec
            - |
              until wget -q -O /dev/null http://discovery:8761/actuator/health; do sleep 2; done
              until wget -q -O /dev/null http://config:8888/actuator/health; do sleep 2; done
              until nc -z postgres 5432; do sleep 2; done
              until nc -z redis 6379; do sleep 2; done
              until nc -z kafka 9092; do sleep 2; done
          resources:
            requests:
              cpu: 5m
              memory: 8Mi
            limits:
              cpu: 50m
              memory: 32Mi
      containers:
        - name: user-service
          image: ghcr.io/prgrms-be-adv-devcourse/prompthub-user-service@sha256:3546f9245791d4741ff90b524103f41759db9e5075180d7fc00487d4d6bbb406
          imagePullPolicy: IfNotPresent
          args:
            - --spring.grpc.client.channel.product-service.target=static://product-service:$(PRODUCT_GRPC_SERVER_PORT)
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: 18081
              protocol: TCP
            - name: grpc
              containerPort: 9081
              protocol: TCP
          env:
            - name: SPRING_CONFIG_IMPORT
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: SPRING_CONFIG_IMPORT
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
            - name: POSTGRES_HOST
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: POSTGRES_HOST
            - name: POSTGRES_PORT
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: POSTGRES_PORT
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              value: user_service
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: USER_SERVICE_PASSWORD
            - name: REDIS_HOST
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: REDIS_PORT
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: USER_SETTLEMENT_KAFKA_LISTENER_ENABLED
              value: "true"
            - name: JWT_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_PRIVATE_KEY
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_PUBLIC_KEY
            - name: USER_GRPC_SERVER_PORT
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: USER_GRPC_SERVER_PORT
            - name: SPRING_GRPC_SERVER_PORT
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: USER_GRPC_SERVER_PORT
            - name: AI_USER_GRPC_TOKEN
              valueFrom:
                secretKeyRef:
                  name: ai-secret
                  key: AI_USER_GRPC_TOKEN
            - name: PRODUCT_GRPC_SERVER_PORT
              valueFrom:
                secretKeyRef:
                  name: runtime-secret
                  key: PRODUCT_GRPC_SERVER_PORT
            - name: MANAGEMENT_ENDPOINT_HEALTH_PROBES_ENABLED
              value: "true"
            - name: JAVA_TOOL_OPTIONS
              value: -XX:InitialRAMPercentage=25.0 -XX:MaxRAMPercentage=70.0 -XX:+ExitOnOutOfMemoryError
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /actuator/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
