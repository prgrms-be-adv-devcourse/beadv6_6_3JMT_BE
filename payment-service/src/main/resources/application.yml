spring:
  application:
    name: payment-service
  config:
    import:
      - optional:file:.env[.properties]
      - ${CONFIG_IMPORT:}
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 1000
        max-interval: 2000
        multiplier: 1.1
