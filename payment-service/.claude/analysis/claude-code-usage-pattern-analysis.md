# Claude Code 사용 패턴 분석 (2026-06-19 ~ 2026-07-19)

**분석일**: 2026-07-19
**데이터 소스**: `~/.claude/projects/*/*.jsonl` 세션 로그 504개
**분석 대상**: 실사용자 프롬프트 1,121개 (task-notification·자동 보안리뷰·compact 이어가기 등 시스템 주입 메시지 제외)

---

## 1. 규모·분포

- 세션 504개, 활동일 26일 (30일 중), 실사용자 프롬프트 1,121개
- 프로젝트 분포: **payment-service 83%** (1,117개), 모노레포 루트 98개, FE 102개 — 사실상 payment-service 단일 집중
- 어시스턴트 메시지 15,989개 — 프롬프트 1개당 평균 약 12개 응답 턴. 짧은 지시로 긴 자율 작업을 시키는 스타일

## 2. 작업 리듬

- 시간대 3피크: **오전 10–12시, 오후 14–16시(최대), 저녁 20–22시**. 18–19시(저녁 시간) 공백 뚜렷
- 요일: 월·목 최다, 주말에도 사용(토+일 236개). 사실상 매일 사용
- 최다 활동일: 6/26 (140개) — 프로젝트 초반 셋업 + 결제 API 구축기

## 3. 세션 스타일 — "짧게 치고 /clear"

- `/clear` **304회**, `/compact` 28회 — 작업 단위마다 컨텍스트를 리셋하는 습관이 강함
- 세션당 프롬프트 중앙값 **2개**, 프롬프트 1개짜리 세션 95개(35%) — 한 세션 = 한 작업
- 예외적으로 긴 세션 최대 58 프롬프트 — 큰 기능(환불 흐름 재설계 등)의 디버깅 루프

## 4. 프롬프트 스타일

- 중앙값 **57자**의 한국어 명령형, "~해줘." 종결
- `@파일참조` 215회(19%) — 계획 문서(`@.claude/plans/...` 61회)·docs·소스를 직접 지정하는 습관
- 긴 프롬프트(>500자) 335개 — 대부분 **요구사항 명세 붙여넣기** 또는 **에러 로그/서버 로그 붙여넣기**. "아래 요구사항 분석하여 구현/계획 세워줘" 패턴이 정형화됨

## 5. 반복 작업 Top

| 작업 | 빈도 |
|---|---|
| 커밋 위임 ("커밋해줘" 계열 자연어) | 약 90회 |
| `/create-branch` | 35 |
| `/create-pr` | 29 |
| `/grill-me` (학습/멘토링 질문) | 27 |
| `/create-issue` | 25 |
| `/superpowers:brainstorming` | 11 |

**핵심 워크플로 = 이슈 생성 → 브랜치 → (브레인스토밍 → 계획 문서) → 구현 → "커밋해줘" → PR.**
git 절차 전반을 커스텀 스킬로 자동화해두고 반복 사용. 팀 컨벤션(한국어 커밋 메시지, merge commit 전략)이 스킬에 박제된 형태.

## 6. 대화 방식 — 계획 주도 + 인터랙티브

- `ExitPlanMode` 118회, `AskUserQuestion` **211회** — 바로 코드를 시키지 않고 계획 승인·선택지 응답으로 조종하는 스타일
- `.claude/plans/` 계획 문서 7개 현존 — 브레인스토밍 결과를 문서로 남기고 `@` 참조로 재사용
- 서브에이전트 246회 호출 (Explore 147회) — 코드 탐색 위임을 적극 활용
- 도구 사용량: Bash 3,158 > Read 2,018 > Edit 1,365 > Write 452

## 7. 토픽 (payment-service 도메인)

| 토픽 | 언급 프롬프트 수 |
|---|---|
| 결제/승인/Toss | 254 |
| 환불 | 111 |
| 테스트 | 85 |
| Kafka/이벤트 | 65 |
| gRPC | 32 |
| Flyway/DB | 26 |
| CI/배포 | 24 |

6월 말 = 결제 승인 구축, 7월 = 환불 흐름 재설계(#398)로 무게 이동.

## 8. 모델 사용 추이

| 주 | 메시지 수 | 주력 모델 |
|---|---|---|
| W25 (6월 3주) | 786 | sonnet-4-6 67%, opus-4-7 32% |
| W26 | 4,448 | sonnet-4-6 96% |
| W27 | 1,203 | sonnet-4-6 75%, fable-5 20% |
| W28 | 4,122 | sonnet-4-6 46%, sonnet-5 29%, opus-4-8 11% |
| W29 (이번 주) | 5,437 | **sonnet-5 81%**, opus-4-7 18% |

`/model` 49회 — 작업 난이도별 모델 스위칭이 잦음.

## 9. 눈에 띄는 갭

1. **트러블슈팅 문서 0개.** `.claude/rules/troubleshooting-doc-format.md`에 자동 작성 규칙까지 만들어놨는데 `payment-service/.claude/trouble-shooting/`가 비어 있음. 에러 로그 붙여넣기 디버깅 세션(버그/디버깅 관련 프롬프트 305개)은 많은데 기록으로 남지 않음 — 규칙과 실행의 괴리.
2. **"커밋해줘" 약 90회 = 순수 자연어 반복.** `/create-commit`은 1회만 사용. 커밋만큼은 스킬 없이 매번 자연어 — 의도적이면 무방하나, 아니라면 자동화 후보 1순위.
3. **테스트 우선 정책 vs 실제.** CLAUDE.md는 TDD 권장인데 `superpowers:test-driven-development` 명시 호출 0회. 테스트는 구현 후 검증용으로 언급되는 패턴이 다수.
4. **긴 로그 통붙여넣기** (최대 31만 자) — 컨텍스트 낭비. 파일로 저장 후 `@` 참조가 더 저렴함.

## 요약

**"계획 문서를 만들고, git 절차를 스킬로 자동화하고, 짧은 한국어 지시 + `/clear`로 세션을 작업 단위로 잘게 끊어 쓰는, payment-service 집중형 계획주도 사용자."**
